# Expo HAS CHANGED

This project is on **Expo SDK 54** (`expo@54.0.36` in `package.json`).

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before
writing any code. Do not read the unversioned or `latest` docs: those track SDK
57, and their APIs will not match what is installed here.

If the app is ever upgraded, change the version in this file at the same time —
a stale link here is worse than none, because it looks authoritative.
